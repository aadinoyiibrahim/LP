name: lp-bench
channels:
  - conda-forge
  - defaults
dependencies:
  # Core runtime
  - python=3.11
  - pandas
  - matplotlib
  - psutil
  - requests
  - scipy

  # Open‑source LP solvers (native libs + Python bindings)
  - highs=1.10.* # CLI + C library
  - highspy=1.10.* # official Python wrapper (matches the same major/minor)
  - glpk=5.*
  - coin-or-clp=1.17.*

  # Modelling DSLs
  - pulp=2.8.*
  - pyomo=6.7.*

  # Optional tools
  - jupyterlab
